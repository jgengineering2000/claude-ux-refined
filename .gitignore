# Runtime — generated by server.py on startup, removed on exit
server/server.port
**/server.port

# Annotation data persisted locally — not part of the shared template
server/annotations/
**/.claude/annotations/
